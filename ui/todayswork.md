# DuukaFlow Project — Admin Analytics

## Goal

Implement AdminAttendancePage, AdminTaxesPage, AdminEmployeeRemunerationPage, AdminBusinessActivityLogs

## Task

Implement those components fully, from rtk to pages to child componets
rtk should reside in src/app/store/features/business, make sure that each model is independent here though,
both of these 4 models, are under base url ==> baseUrl: `${import.meta.env.VITE_BASE_URL}/admin`,

## Constraints

- ✅ Use real data as it's being fetched rtk
- ✅ Split code into components (no bloated parent).
- ✅ Use shadcn/ui for layout consistency.
- ✅ Keep UI/UX compact and dashboard-friendly.
- ✅ Render Components in the parent w.
- ✅ Follow the implemented architecture.
- ✅ No hallucination — follow existing project patterns.

**Important** Take advanced decision in implementing this feature, and run commands to where needed
