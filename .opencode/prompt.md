# Today's work

## Subscriptions

- Complete the business payment for the services on the front end. Here, follow the ui pattern ie
  -> The subscriptions should be in a table
  -> The running subscription should be highlighted
  -> The add new subscription button should be on top of the table header on the upper right
  -> Render a day countdown when the user pays, this should count till the subscription expires

## Add a superadmin dashboard

Here, you'll follow the pattern of other dashboards to include

- CRUD of the subscriptions
- Take action of new subscription_payments (completed status marking). Here the super admin checks and verifies payment as completed
-

## DuukaFlow Agent

Here, put the system prompt in its file if possible.

- Make the prompt stronger so the the agent doesn't hallucinate
- On the greeting, where it mentions the user's name, remove the "@" concatenated manually, just render user_name as is

### Constraints

- Do not hallucinate
- Work as a senior staff engineer
- Implement scalable approaches
- Include comments in your work
